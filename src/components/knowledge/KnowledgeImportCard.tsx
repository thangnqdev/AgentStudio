export function KnowledgeImportCard({ isImporting, notice, onImport }: { isImporting: boolean; notice: string; onImport: () => void }) {
  return <section className="rounded-2xl border border-dashed border-outline bg-surface-container-low p-6"><div className="flex h-full flex-col"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-secondary text-on-secondary shadow-sm"><span className="material-symbols-outlined">upload_file</span></div><h3 className="mt-5 font-display-serif text-[25px] text-primary">Nạp nguồn</h3><p className="mt-2 text-[13px] leading-5 text-on-surface-variant">Chọn Markdown, TXT, JSON, CSV, HTML, YAML và mã nguồn văn bản. File dialog xác nhận nguồn trước khi đọc.</p><button onClick={onImport} disabled={isImporting} className="mt-6 inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 font-ui-label-bold text-[13px] text-on-primary transition hover:bg-primary/90 disabled:opacity-60"><span className={`material-symbols-outlined text-[18px] ${isImporting ? 'animate-spin' : ''}`}>{isImporting ? 'sync' : 'add'}</span>{isImporting ? 'Đang lập chỉ mục' : 'Thêm tài liệu'}</button><p className="mt-3 text-center font-code-base text-[10px] text-on-surface-variant">tối đa 5 MB / tệp</p>{notice && <p className="mt-4 rounded-lg bg-surface px-3 py-2 text-[12px] leading-5 text-on-surface-variant">{notice}</p>}</div></section>;
}
