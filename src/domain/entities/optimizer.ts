export type RuntimeOptimizationConfig = { retrievalTopK: number; lexicalWeight: number; semanticWeight: number; modelChoice: string | null; contextBudgetTokens: number; retryCount: number; timeoutMs: number; skillRankingWeight: number };
export type OptimizationEvaluation = { version: 1; evaluatorId: string; evaluatorVersion: string; baselineRunId: string; candidateRunId: string; baselineScore: number; candidateScore: number; improvement: number; minimumImprovement: number; passed: boolean; configurationDigest: string; evaluatedAt: string };
export type OptimizationCandidate = { id: string; baseRevision: number; config: RuntimeOptimizationConfig; changedKeys: Array<keyof RuntimeOptimizationConfig>; status: 'draft' | 'evaluated' | 'promoted' | 'rejected'; createdAt: string; evaluation?: OptimizationEvaluation };
export type OptimizerState = { version: 1; revision: number; active: RuntimeOptimizationConfig; candidates: OptimizationCandidate[]; history: Array<{ revision: number; config: RuntimeOptimizationConfig; reason: 'promotion' | 'rollback'; candidateId?: string; activatedAt: string }> };
