export type SkillCandidateEvaluation = { version: 1; evaluatorId: string; evaluatorVersion: string; candidateDigest: string; passed: boolean; results: Array<{ testId: string; passed: boolean; message: string }>; evaluatedAt: string };
export type SkillCandidate = { schemaVersion: 1; id: string; sourceTraceId: string; sourceTraceStatus: 'succeeded'; name: string; description: string; skillVersion: string; instructions: string; toolSequence: string[]; tests: Array<{ id: string; version: 1; kind: string; description: string }>; status: 'draft' | 'evaluated' | 'approved' | 'rejected' | 'promoted'; createdAt: string; evaluation?: SkillCandidateEvaluation; approval?: { candidateDigest: string; decision: 'approved' | 'rejected'; approvedBy: 'local-user'; decidedAt: string }; promotion?: { skillName: string; skillVersion: string; algorithm: 'hmac-sha256'; signature: string; candidateDigest: string; promotedAt: string } };
